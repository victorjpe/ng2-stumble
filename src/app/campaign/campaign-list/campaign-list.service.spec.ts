/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CampaignListService } from './campaign-list.service';

describe('CampaignListService', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [CampaignListService]
        });
    });

    // it('should be valid', inject([CampaignListService], (service: CampaignListService) => {
    //   expect(service).toBeTruthy();
    // }));

    // it('Campaign should get 2 elements', inject([CampaignListService], (service: CampaignListService) => {
    //   service.getCampaigns().subscribe(c => console.log(c));
    //   expect(1 == 1);
    // }));

    // it('abc function should return 4 elements', inject([CampaignListService], (service: CampaignListService) => {
    //   expect(service.abc().length == 4);
    // }));




});
